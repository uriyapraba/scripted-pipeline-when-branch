node
{
    if(env.BRANCH_NAME == 'master')
    {
        stage('Build-master')
        {
            println "Building from master"
        }
    }
    if(env.BRANCH_NAME == 'dev')
    {
        stage('Build-dev')
        {
            println "Building from Dev"
        }
    }
}